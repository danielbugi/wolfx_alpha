mechanism/instructions.txt — see the authoritative docs instead of this file
==============================================================================

This file described a manual, five-phase execution order using scripts that never existed under
these names (`daily_data_updater_corrected.py`, `fundamentals_updater_corrected.py`,
`historical_breakouts_corrected.py`, `donchian_screener_corrected.py` — confirmed by the 2026-09-25
codebase audit: these `*_corrected.py` names are not found anywhere in repo history) and a runner
(`master_automation_runner_updated.py`) not wired into the live pipeline. Replaced 2026-09-27 rather
than kept in sync, since a second, drifting copy of this information is exactly what caused it to go
stale in the first place.

The real, current way to run the pipeline:
    ./automation_pipeline.sh            # full 13-step pipeline; skips itself on non-trading days
    ./automation_pipeline.sh --force    # run anyway

See CLAUDE.md's "Commands quick reference" and docs/dev/LOCAL_SETUP.md for the full set of manual
commands (individual updater/screener test runs, Telegram dry-runs, etc.), and
docs/architecture/SCHEDULING.md for how this actually gets triggered in production (VPS systemd
timers, not a manual phase-by-phase run).

The pre-2026-09-27 version of this file, if you need the historical detail, is in git history.
